name: Release

env:
  IMAGE_TAG: ${{ github.ref_name }}

on:
  push:
    tags:
      - '*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      - run: make release
